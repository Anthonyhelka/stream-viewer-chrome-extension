import { environment } from '../../environments/environment';

export const baseUrl = environment.baseUrl;
export const wsServerUrl = environment.websocketUrl;
export const baseSiteUrl = environment.baseSiteUrl;
